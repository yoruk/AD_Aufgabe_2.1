public class Simulation {
  //Simulation SETUP  
	DECLARE static final int  N = 5;;
	DECLARE static final int NUMBOXINGPLANTS = 5;
	DECLARE static final int ORDERMAXSIZE = 50;
	DECLARE static final int MAXCAPACITY = ORDERMAXSIZE;
	DECLARE static final int NUMROBOTS = NUMBOXINGPLANTS;
	DECLARE static final int PPTIME = 5;
	DECLARE static final boolean TEST = false;
    //main()
    public static void main(String[] args) {
 
       FOR (DECLARE int k = 0; k < 1;k++) 
            PRINT "Durchlauf:"+k;

            DECLARE List<Item> item = CALL Item.factory();
           DECLARE Warehouse wh = NEWOBJECT WarehouseImpl(item);
        

        
		DECLARE Order order = NEWOBJECT Order();
		CALL order.addItem(CALL item.get(0), 1);
		CALL wh.takeOrder(CALL order.list());
       
       DECLARE Order order2 = NEWOBJECT Order();
       CALL order2.addItem(CALL item.get(0), 1);
       CALL wh.takeOrder(CALL order2.list());
        
		DECLARE  Order order3 = NEWOBJECT Order();
       CALL order3.addItem(CALL item.get(4), 1);
       CALL wh.takeOrder(CALL order3.list());
              
		DECLARE Order order4 = NEWOBJECT Order();
       CALL order4.addItem(CALL item.get(8), 1);
       CALL wh.takeOrder(CALL order4.list());
       
       PRINT CALL wh.toString();
       
		DECLARE  int i=0;
       while (CALL !wh.done() && i < 3000000) {
           wh.action();

           PRINT CALL wh.toString();
           PRINT
		   PRINT

           CALL Thread.sleep(100);

           i++;
       }
        
		PRINT i;
       SPRINT CALL wh.done();
       PRINT wh;
        
       }
           
        END FOR
 
    END MAIN()
END CLASS